import React from 'react';
import { Button as WebButton } from '@material-ui/core';

const Button = () => (
  <WebButton variant="contained" color="primary" disableElevation>Web Button</WebButton>
);

export default Button;